import React from "react";
import { ContentProps } from "@/types/global-types";

const Home: React.FC<ContentProps> = () => {
	return <></>;
};

export default Home;
export { Home as HomePage, Home as HomeSection };
